# Required metadata
sonar.projectKey=monahar
sonar.projectName=Simple Project
sonar.projectVersion=1.0

# Description of project (optional)
sonar.projectDescription=Simple project analyzed using the SonarQube Runner

# Language
sonar.language=java

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Cobol specific properties
# Comma-separated paths to directories with copybooks

# Comma-separated list of suffixes


# Encoding of the source files
sonar.sourceEncoding=UTF-8